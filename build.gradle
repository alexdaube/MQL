buildscript {
    repositories {
        mavenCentral()
        jcenter()
    }
}

allprojects {
    apply plugin: "idea"
    version '1.0-SNAPSHOT'
    repositories {
        mavenCentral()
    }
}

project(":mql-service") {
    apply plugin: "java"
    dependencies {
    }
}